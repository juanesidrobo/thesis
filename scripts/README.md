# scripts/

Carpeta de utilidades y scripts de apoyo del repositorio.

## Contenido

| Archivo | Descripción |
|---|---|
| `compilar.sh` | Compila la tesis con un solo comando: `bash scripts/compilar.sh`. |

> 💡 También puedes compilar desde la raíz con `make` o desde `documentacion/` con `make`.
